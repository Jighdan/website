---
import { AstroSeo } from '@astrolib/seo';

interface Props {
	title: string;
	description: string;
}

const { title, description } = Astro.props;
---

<AstroSeo
	title={title}
	description={description}
	canonical={Astro.url.toString()}
	openGraph={{
		title,
		description,
		url: Astro.url.toString(),
		images: [
			{
				url: "/landmark.png",
				alt: "Reinny Almonte Ramos",
			}
		]
	}}
/>